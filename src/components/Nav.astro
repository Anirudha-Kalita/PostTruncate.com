---
// Sticky brand navigation. Logo left · anchor links center · actions right
// (language switcher + theme toggle always visible; CTA on desktop, hamburger
// on mobile). Both disclosure menus are CSS-first <details> elements; the
// script below only adds close-on-navigate / outside-tap / Escape as polish.
import { LOCALES, getLocale } from '../i18n/config';
import { getTranslations } from '../i18n';
import { interp } from '../i18n/interp';
import type { Translations } from '../i18n/types';
import { tools } from '../data/tools';

interface Props {
  /** Active locale code — drives the localized links and current-language UI. */
  lang: string;
  /** Translated nav strings. */
  nav: Translations['nav'];
}

const { lang, nav } = Astro.props;
const current = getLocale(lang);
const t = getTranslations(lang);

// Section anchors are made absolute (prefixed with the locale home) so they
// resolve from any page — on a standalone route like /about/ a bare "#faq"
// would point nowhere. About/Contact are real locale-prefixed routes.
const home = `/${lang}/${current.slug}/`;
const links = [
  { href: `${home}#workspace`, label: nav.links.editor },
  // "Platform Guides" is now a dropdown — handled separately below.
  { href: `/${lang}/faq/`, label: nav.links.faq },
  { href: `${home}about/`, label: nav.links.about },
  { href: `${home}contact/`, label: nav.links.contact },
];

// Reference page surfaced at the top of the Platform Guides dropdown.
const limitsLink = { href: `/${lang}/platform-limits/`, label: nav.links.limits };

// Platform Guides dropdown items — URLs from tools.ts, labels from i18n.
const PLATFORM_LOGOS: Record<string, string> = {
  twitter:     '/logos/twitter.svg',
  instagram:   '/logos/instagram.svg',
  linkedin:    '/logos/linkedin.svg',
  facebook:    '/logos/facebook.svg',
  sms:         '/logos/sms.svg',
  threads:     '/logos/threads.svg',
  'google-serp': '/logos/google-serp.svg',
};
const guidesLinks = tools.map((tool, i) => {
  const label = t.footer.columns.guides.links[i];
  return {
    label,
    logoAlt: interp(t.images.platformLogo, { platform: label }),
    href: `/${lang}/${tool.slugs[lang] ?? tool.slugs['en']}/`,
    logo: PLATFORM_LOGOS[tool.id] ?? '/logos/google-serp.svg',
  };
});
---

<header class="nav">
  <div class="container-page nav__inner">
    <a href={home} class="nav__brand" aria-label={nav.homeAria}>
      <img class="nav__mark" src="/favicon.svg" alt={t.images.logoAlt} width="22" height="22" />
      <span class="nav__word">PostTruncate</span>
    </a>

    <nav class="nav__links" aria-label="Primary">
      {/* First link (Editor) */}
      <a href={links[0].href} class="nav__link">{links[0].label}</a>

      {/* Platform Guides dropdown */}
      <details class="nav__guides" id="nav-guides-desktop">
        <summary class="nav__link nav__guides-toggle">
          {nav.links.guides}
          <svg class="nav__guides-chevron" width="10" height="10" viewBox="0 0 10 10" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M2.5 3.5 5 6.5 7.5 3.5" />
          </svg>
        </summary>
        <div class="nav__guides-panel">
          <p class="nav__guides-heading">{nav.links.guides}</p>
          <ul class="nav__guides-list">
            <li>
              <a href={limitsLink.href} class="nav__guides-link nav__guides-link--all">
                <span>{limitsLink.label}</span>
              </a>
            </li>
            {guidesLinks.map((gl) => (
              <li>
                <a href={gl.href} class="nav__guides-link">
                  <img src={gl.logo} width="20" height="20" alt={gl.logoAlt} class="nav__guides-logo" loading="lazy" decoding="async" />
                  <span>{gl.label}</span>
                </a>
              </li>
            ))}
          </ul>
        </div>
      </details>

      {/* Remaining links (FAQ, About, Contact) */}
      {links.slice(1).map((l) => <a href={l.href} class="nav__link">{l.label}</a>)}
    </nav>

    <div class="nav__actions">
      <!-- Language switcher (shown at every breakpoint). CSS-only <details>;
           closes on navigate / outside-tap / Escape via the script below. -->
      <details class="nav__lang">
        <summary class="nav__lang-toggle" aria-label={nav.languageAria}>
          <svg
            width="18"
            height="18"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
            aria-hidden="true"
          >
            <circle cx="12" cy="12" r="9"></circle>
            <path d="M3 12h18"></path>
            <path d="M12 3a14 14 0 0 1 0 18 14 14 0 0 1 0-18"></path>
          </svg>
          <span class="nav__lang-code">{current.code.toUpperCase()}</span>
        </summary>
        <div class="nav__lang-panel">
          <p class="nav__lang-heading">{nav.language}</p>
          <ul class="nav__lang-list">
            {
              LOCALES.map((l) => (
                <li>
                  <a
                    href={`/${l.code}/${l.slug}/`}
                    class="nav__lang-link"
                    hreflang={l.code}
                    lang={l.code}
                    aria-current={l.code === lang ? 'true' : undefined}
                  >
                    <span class="nav__lang-option">
                      <img
                        class="nav__lang-flag"
                        src={l.flagSrc}
                        alt={l.label}
                        width="22"
                        height="16"
                        loading="lazy"
                        decoding="async"
                      />
                      <span class="nav__lang-name">{l.label}</span>
                    </span>
                    <span class="nav__lang-tag">{l.code.toUpperCase()}</span>
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </details>

      <button
        type="button"
        class="nav__theme"
        aria-label={nav.themeToDark}
        aria-pressed="false"
        data-to-dark={nav.themeToDark}
        data-to-light={nav.themeToLight}
      >
        <svg
          class="nav__sun"
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
        >
          <circle cx="12" cy="12" r="4"></circle>
          <path
            d="M12 2v2M12 20v2M4.93 4.93l1.41 1.41M17.66 17.66l1.41 1.41M2 12h2M20 12h2M4.93 19.07l1.41-1.41M17.66 6.34l1.41-1.41"
          ></path>
        </svg>
        <svg
          class="nav__moon"
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
          stroke-linecap="round"
          stroke-linejoin="round"
          aria-hidden="true"
        >
          <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path>
        </svg>
      </button>

      <div class="nav__cta">
        <a href={`${home}#workspace`} class="nav__pill">{nav.cta}</a>
      </div>

      <!-- Mobile disclosure menu (shown < md). CSS-only baseline; the script
           below just closes it on navigate / outside tap as enhancement. -->
      <details class="nav__menu">
        <summary class="nav__toggle" aria-label={nav.menuAria}>
          <span class="nav__bars" aria-hidden="true">
            <span></span><span></span><span></span>
          </span>
        </summary>
        <div class="nav__panel">
          <nav class="nav__panel-links" aria-label="Primary mobile">
            {/* First link (Editor) */}
            <a href={links[0].href} class="nav__panel-link">{links[0].label}</a>

            {/* Platform Guides — inline expand */}
            <details class="nav__guides-mobile" id="nav-guides-mobile">
              <summary class="nav__panel-link nav__guides-mobile-toggle">
                {nav.links.guides}
                <svg class="nav__guides-chevron" width="12" height="12" viewBox="0 0 10 10" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <path d="M2.5 3.5 5 6.5 7.5 3.5" />
                </svg>
              </summary>
              <div class="nav__guides-mobile-panel">
                <a href={limitsLink.href} class="nav__panel-link nav__guides-mobile-link nav__guides-mobile-link--all">
                  <span>{limitsLink.label}</span>
                </a>
                {guidesLinks.map((gl) => (
                  <a href={gl.href} class="nav__panel-link nav__guides-mobile-link">
                    <img src={gl.logo} width="20" height="20" alt={gl.logoAlt} class="nav__guides-logo" loading="lazy" decoding="async" />
                    <span>{gl.label}</span>
                  </a>
                ))}
              </div>
            </details>

            {/* Remaining links (FAQ, About, Contact) */}
            {links.slice(1).map((l) => <a href={l.href} class="nav__panel-link">{l.label}</a>)}
          </nav>
          <a href={`${home}#workspace`} class="nav__pill nav__pill--block">{nav.cta}</a>
        </div>
      </details>
    </div>
  </div>
</header>

<script>
  // Close-on-navigate / outside-tap / Escape for every CSS-first disclosure in
  // the nav (the mobile menu, language switcher, and guides dropdown share
  // this behavior).
  const disclosures = Array.from(
    document.querySelectorAll<HTMLDetailsElement>(
      '.nav__menu, .nav__lang, .nav__guides, .nav__guides-mobile'
    )
  );
  const LANGUAGE_COOKIE = 'preferred_locale';

  const persistLanguagePreference = (link: HTMLAnchorElement) => {
    const lang = link.getAttribute('hreflang')?.toLowerCase();
    if (!lang) return;

    try {
      localStorage.setItem('lang', lang);
    } catch {}

    document.cookie =
      LANGUAGE_COOKIE +
      '=' +
      encodeURIComponent(lang) +
      '; Path=/; Max-Age=31536000; SameSite=Lax';
  };

  for (const d of disclosures) {
    d.querySelectorAll('a').forEach((a) =>
      a.addEventListener('click', () => {
        if (a.matches('.nav__lang-link')) persistLanguagePreference(a);
        d.removeAttribute('open');
      })
    );
  }
  document.addEventListener('click', (e) => {
    for (const d of disclosures) {
      if (d.open && !d.contains(e.target as Node)) d.removeAttribute('open');
    }
  });
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape')
      for (const d of disclosures) d.removeAttribute('open');
  });

  // ── Guides dropdown: open on hover (desktop) ───────────────────────────
  const guidesDesktop = document.querySelector<HTMLDetailsElement>('.nav__guides');
  if (guidesDesktop) {
    let hoverTimeout: ReturnType<typeof setTimeout> | undefined;
    guidesDesktop.addEventListener('mouseenter', () => {
      clearTimeout(hoverTimeout);
      guidesDesktop.setAttribute('open', '');
    });
    guidesDesktop.addEventListener('mouseleave', () => {
      hoverTimeout = setTimeout(() => guidesDesktop.removeAttribute('open'), 150);
    });
  }

  // ── Theme toggle ────────────────────────────────────────────────────────
  // The initial theme is applied pre-paint by the inline script in Layout
  // (no flash). Here we only handle the click: flip data-theme, persist the
  // choice, and keep the button's a11y state + the address-bar theme-color
  // in sync. Stored value wins over the OS preference once the user chooses.
  const root = document.documentElement;
  const btn = document.querySelector<HTMLButtonElement>('.nav__theme');
  const meta = document.querySelector<HTMLMetaElement>('meta[name="theme-color"]');
  const THEME_COLOR = { light: '#ffffff', dark: '#0a0a0a' } as const;
  // Localized aria-labels handed down from the server (avoids hardcoding English).
  const LABELS = {
    toDark: btn?.dataset.toDark ?? 'Switch to dark theme',
    toLight: btn?.dataset.toLight ?? 'Switch to light theme',
  };

  const syncButton = (theme: 'light' | 'dark') => {
    if (!btn) return;
    const dark = theme === 'dark';
    btn.setAttribute('aria-pressed', String(dark));
    btn.setAttribute('aria-label', dark ? LABELS.toLight : LABELS.toDark);
    if (meta) meta.setAttribute('content', THEME_COLOR[theme]);
  };

  syncButton(root.dataset.theme === 'dark' ? 'dark' : 'light');

  btn?.addEventListener('click', () => {
    const next = root.dataset.theme === 'dark' ? 'light' : 'dark';
    root.dataset.theme = next;
    try {
      localStorage.setItem('theme', next);
    } catch {}
    syncButton(next);
  });

  // Follow OS changes only while the user hasn't made an explicit choice.
  window
    .matchMedia('(prefers-color-scheme: dark)')
    .addEventListener('change', (e) => {
      let stored: string | null = null;
      try {
        stored = localStorage.getItem('theme');
      } catch {}
      if (stored) return;
      const theme = e.matches ? 'dark' : 'light';
      root.dataset.theme = theme;
      syncButton(theme);
    });
</script>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 50;
    height: 64px;
    background: color-mix(in srgb, var(--color-canvas) 80%, transparent);
    backdrop-filter: saturate(180%) blur(12px);
    border-bottom: 1px solid var(--color-hairline);
  }
  .nav__inner {
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .nav__brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--color-ink);
    font-weight: 600;
    font-size: 16px;
    letter-spacing: -0.4px;
    flex-shrink: 0;
  }
  .nav__mark {
    width: 22px;
    height: 22px;
    border-radius: var(--radius-sm);
    box-shadow: var(--shadow-e1);
    display: block;
  }

  .nav__links {
    display: none;
    align-items: center;
    gap: 4px;
  }
  .nav__link {
    padding: 8px 12px;
    border-radius: var(--radius-pill);
    color: var(--color-body);
    font-size: 14px;
    letter-spacing: -0.28px;
    transition: color 0.15s ease, background 0.15s ease;
  }
  .nav__link:hover {
    color: var(--color-ink);
    background: var(--color-canvas-soft-2);
  }

  /* Right-side actions cluster — language + theme (always shown) + CTA / menu. */
  .nav__actions {
    display: flex;
    align-items: center;
    gap: 8px;
    flex-shrink: 0;
  }

  /* ── Language switcher ──────────────────────────────────────────────── */
  .nav__lang {
    position: relative;
    flex-shrink: 0;
  }
  .nav__lang-toggle {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    height: 40px;
    padding: 0 10px;
    border-radius: var(--radius-md);
    cursor: pointer;
    list-style: none;
    color: var(--color-body);
    transition: transform 100ms ease, color 0.15s ease, background 0.15s ease;
  }
  .nav__lang-toggle:hover {
    color: var(--color-ink);
    background: var(--color-canvas-soft-2);
  }
  .nav__lang-toggle:active {
    transform: scale(0.94);
  }
  .nav__lang-toggle::-webkit-details-marker {
    display: none;
  }
  .nav__lang-code {
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.02em;
    font-variant-numeric: tabular-nums;
  }
  .nav__lang-panel {
    position: absolute;
    top: calc(100% + 8px);
    right: 0;
    width: 220px;
    max-height: min(70vh, 420px);
    overflow-y: auto;
    padding: 8px;
    background: var(--color-canvas);
    border: 1px solid var(--color-hairline);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-e4);
  }
  .nav__lang-heading {
    padding: 6px 10px;
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-mute);
  }
  .nav__lang-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 2px;
  }
  .nav__lang-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 9px 10px;
    border-radius: var(--radius-md);
    color: var(--color-ink);
    font-size: 14px;
    transition: background 0.15s ease;
  }
  .nav__lang-link:hover {
    background: var(--color-canvas-soft-2);
  }
  .nav__lang-link[aria-current='true'] {
    background: var(--color-canvas-soft-2);
    font-weight: 600;
  }
  .nav__lang-option {
    display: inline-flex;
    min-width: 0;
    align-items: center;
    gap: 10px;
  }
  .nav__lang-flag {
    width: 22px;
    height: 16px;
    flex-shrink: 0;
    display: block;
    object-fit: cover;
    border-radius: 2px;
    box-shadow: 0 0 0 1px color-mix(in srgb, var(--color-ink) 12%, transparent);
  }
  .nav__lang-name {
    min-width: 0;
    overflow-wrap: anywhere;
  }
  .nav__lang-tag {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--color-mute);
    font-variant-numeric: tabular-nums;
  }

  /* Theme toggle — shown at every breakpoint. Sun in light, moon in dark. */
  .nav__theme {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    color: var(--color-body);
    background: transparent;
    border: none;
    cursor: pointer;
    transition: transform 100ms ease, color 0.15s ease, background 0.15s ease;
  }
  .nav__theme:hover {
    color: var(--color-ink);
    background: var(--color-canvas-soft-2);
  }
  .nav__theme:active {
    transform: scale(0.9);
  }
  .nav__moon {
    display: none;
  }
  :global(html[data-theme='dark']) .nav__sun {
    display: none;
  }
  :global(html[data-theme='dark']) .nav__moon {
    display: block;
  }

  /* Desktop CTA — hidden on mobile, where it lives inside the menu panel. */
  .nav__cta {
    display: none;
    flex-shrink: 0;
  }
  .nav__pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    height: 36px;
    padding: 0 16px;
    border-radius: var(--radius-pill);
    background-color: var(--color-link);
    color: var(--color-on-primary);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: -0.28px;
    transition: transform 100ms ease, background-color 0.15s ease, opacity 0.15s ease;
  }
  .nav__pill:hover {
    background-color: var(--color-link-deep);
  }
  .nav__pill:active {
    transform: scale(0.955);
    opacity: 0.82;
  }
  .nav__pill--block {
    width: 100%;
    height: 44px;
  }

  /* ── Mobile disclosure menu (< md) ──────────────────────────────────── */
  .nav__menu {
    flex-shrink: 0;
  }
  .nav__toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    border-radius: var(--radius-md);
    cursor: pointer;
    list-style: none;
    color: var(--color-ink);
    transition: transform 100ms ease, background 0.15s ease;
  }
  .nav__toggle:hover {
    background: var(--color-canvas-soft-2);
  }
  .nav__toggle:active {
    transform: scale(0.9);
  }
  /* Strip the native disclosure triangle across engines. */
  .nav__toggle::-webkit-details-marker {
    display: none;
  }
  .nav__bars {
    position: relative;
    display: block;
    width: 18px;
    height: 12px;
  }
  .nav__bars span {
    position: absolute;
    left: 0;
    width: 100%;
    height: 2px;
    border-radius: 2px;
    background: currentColor;
    transition: transform 0.2s ease, opacity 0.2s ease;
  }
  .nav__bars span:nth-child(1) { top: 0; }
  .nav__bars span:nth-child(2) { top: 5px; }
  .nav__bars span:nth-child(3) { top: 10px; }
  /* Morph to an X when open. */
  .nav__menu[open] .nav__bars span:nth-child(1) { transform: translateY(5px) rotate(45deg); }
  .nav__menu[open] .nav__bars span:nth-child(2) { opacity: 0; }
  .nav__menu[open] .nav__bars span:nth-child(3) { transform: translateY(-5px) rotate(-45deg); }

  .nav__panel {
    position: absolute;
    top: 64px;
    left: 0;
    right: 0;
    display: flex;
    flex-direction: column;
    gap: 6px;
    padding: 12px 16px 16px;
    background: var(--color-canvas);
    border-bottom: 1px solid var(--color-hairline);
    box-shadow: var(--shadow-e3);
  }
  .nav__panel-links {
    display: flex;
    flex-direction: column;
    gap: 2px;
    margin-bottom: 8px;
  }
  .nav__panel-link {
    padding: 12px 12px;
    border-radius: var(--radius-md);
    color: var(--color-ink);
    font-size: 15px;
    font-weight: 500;
    letter-spacing: -0.28px;
    transition: transform 100ms ease, background 0.15s ease;
  }
  .nav__panel-link:hover {
    background: var(--color-canvas-soft-2);
  }
  .nav__panel-link:active {
    transform: scale(0.975);
    background: var(--color-canvas-soft-2);
  }

  /* ── Platform Guides dropdown (desktop ≥ md) ──────────────────────── */
  .nav__guides {
    position: relative;
    flex-shrink: 0;
  }
  .nav__guides-toggle {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    cursor: pointer;
    list-style: none;
  }
  .nav__guides-toggle::-webkit-details-marker {
    display: none;
  }
  .nav__guides-chevron {
    transition: transform 0.2s ease;
    flex-shrink: 0;
  }
  .nav__guides[open] .nav__guides-chevron {
    transform: rotate(180deg);
  }
  .nav__guides-panel {
    position: absolute;
    top: calc(100% + 8px);
    left: 50%;
    transform: translateX(-50%);
    width: 240px;
    max-height: min(70vh, 420px);
    overflow-y: auto;
    padding: 8px;
    background: var(--color-canvas);
    border: 1px solid var(--color-hairline);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-e4);
    z-index: 60;
  }
  .nav__guides-heading {
    padding: 6px 10px;
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--color-mute);
  }
  .nav__guides-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 2px;
  }
  .nav__guides-link {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 9px 10px;
    border-radius: var(--radius-md);
    color: var(--color-ink);
    font-size: 14px;
    transition: background 0.15s ease;
  }
  .nav__guides-link:hover {
    background: var(--color-canvas-soft-2);
  }
  .nav__guides-logo {
    flex-shrink: 0;
    width: 20px;
    height: 20px;
    border-radius: 4px;
    display: block;
  }
  /* "All platform limits" reference row — separated from the brand rows. */
  .nav__guides-link--all {
    font-weight: 500;
    border-bottom: 1px solid var(--color-hairline);
    border-radius: var(--radius-md) var(--radius-md) 0 0;
    margin-bottom: 4px;
  }

  /* ── Platform Guides mobile inline expand ───────────────────────────── */
  .nav__guides-mobile {
    /* sits inline within the mobile panel links */
  }
  .nav__guides-mobile-toggle {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    cursor: pointer;
    list-style: none;
  }
  .nav__guides-mobile-toggle::-webkit-details-marker {
    display: none;
  }
  .nav__guides-mobile[open] .nav__guides-chevron {
    transform: rotate(180deg);
  }
  .nav__guides-mobile-panel {
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding-left: 16px;
    margin-top: 2px;
  }
  .nav__guides-mobile-link {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 14px !important;
    padding: 10px 12px !important;
    color: var(--color-body) !important;
    font-weight: 400 !important;
  }
  .nav__guides-mobile-link:hover {
    color: var(--color-ink) !important;
  }

  /* Hide desktop guides dropdown and show mobile version at < md. */
  .nav__guides {
    display: none;
  }
  .nav__guides-mobile {
    display: block;
  }

  @media (min-width: 768px) {
    .nav__links {
      display: flex;
    }
    .nav__cta {
      display: block;
    }
    .nav__menu {
      display: none;
    }
    .nav__guides {
      display: block;
    }
    .nav__guides-mobile {
      display: none;
    }
  }
</style>
